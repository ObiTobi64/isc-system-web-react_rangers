name: Frontend CI/CD
on: [push]
jobs:
  test-build-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Setup Node
      uses: actions/setup-node@v2
      with:
        node-version: 18
    - run: npm install
    - run: npm test
    - name: Build  # Opcional: build en runner para testear
      run: npm run build
    - name: Deploy to VM1 (con SSH y Nginx)
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.VM1_HOST }}
        username: ${{ secrets.VM1_USER }}
        key: ${{ secrets.DEPLOY_KEY }}
        script: |
          REPO_DIR=~/isc-system-web-react_rangers
          APP_DIR=$REPO_DIR/isc-system-web
          if [ ! -d "$REPO_DIR" ]; then
            echo "Clonando repo por primera vez..."
            git clone https://github.com/ObiTobi64/isc-system-web-react_rangers.git $REPO_DIR
          fi
          cd $APP_DIR
          git fetch --all
          git reset --hard origin/main  # Fuerza código fresco (borra cambios locales)
          npm install--omit=dev  # Instala solo prod deps (ignora WARN)
          npm run build
          sudo rm -rf /var/www/html/*
          sudo cp -r dist/* /var/www/html/
          sudo chown -R www-data:www-data /var/www/html/
          sudo chmod -R 755 /var/www/html/
          sudo find /var/www/html/ -type f -exec chmod 644 {} \;
          sudo systemctl reload nginx
          echo "Deploy UPB completado en $(date)"